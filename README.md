# red-badger-test

**Get Started**

    Install node modules:

    ```shell
     npm install
    ```

**Run the Program**

    Run the program with node:

    ```shell
     node index.js
    ```
**Run the tests**

    Run the Jest Tests:

    ```shell
     npm run Jest
    ```
